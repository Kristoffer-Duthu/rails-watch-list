<h1 class="header"> <%= @list.name %> </h1>

<% @list.movies.each do |movie| %>
<p> <%= movie.title %> </p>
<% movie.bookmarks.each do |b| %>
<p> <%= b.comment %> <%= link_to "Delete Bookmark", bookmark_path(b), method: :delete, class: "btn btn-outline-danger" %></p>
<% end %>
<% end %>

<%= link_to "Add new Bookmark", new_list_bookmark_path(@list), class: "btn btn-outline-secondary" %> ||
<%= link_to "Back to Lists", lists_path(@lists), class: "btn btn-outline-primary" %>
